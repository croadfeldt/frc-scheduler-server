# Suggested TBA event keys for the eval inventory.
#
# Lines starting with '#' are ignored. Inline comments after a key are
# also stripped (the key is the first whitespace-separated token).
#
# For broader coverage, prefer the year+state expansion which queries
# TBA for the actual event list (no guesswork on key spelling):
#
#   python -m scripts.scheduler_eval.pull_tba_fixtures --year 2024 --state MN
#   python -m scripts.scheduler_eval.pull_tba_fixtures --year 2025 --state MN
#   python -m scripts.scheduler_eval.pull_tba_fixtures --year 2026 --state MN
#
# This file is for hand-curated additions or for events outside the
# year+state bracket. Only keys verified-existing on TBA are listed.

# ── Minnesota events (closest analog to 2026mnst) ─────────────────────
# State Tournament 2023-2026
2023mnst
2024mnst
2025mnst
2026mnst

# Northern Lights regional (Duluth)
2023mndu
2024mndu
2025mndu
2026mndu

# 10000 Lakes regional (Minneapolis area)
2023mnmi
2024mnmi
2025mnmi

# ── Larger fields (60+ teams) — Michigan state championships ─────────
# These stress the algorithm differently — more team pairs to
# diversify across, more matches to sequence.
# Michigan splits its state championship into divisions; use the
# divisional keys (2024micmp1-4), not a non-existent umbrella key.
2024micmp1
2024micmp2
2024micmp3
2024micmp4

# ── Mid-size offseason events ────────────────────────────────────────
# Verified-existing weeks-zero / off-season events.
2024week0

# ── Add your own below ───────────────────────────────────────────────
# To check if a key is valid on TBA before adding it:
#   curl -H "X-TBA-Auth-Key: $TBA_API_KEY" \
#        https://www.thebluealliance.com/api/v3/event/<KEY>/simple

