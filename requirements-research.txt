# Research-only dependencies. NOT installed in the production
# container by default. Required for scripts under `scripts/cp_sat/`
# and any other research/investigation work that needs additional
# tooling.
#
# To install:
#     pip install -r requirements-research.txt
#
# All listed packages are GPL-3 compatible.
#
# Note (2026-05-14): `ortools` was previously listed here, then
# promoted to `requirements.txt` (production) when CP-SAT became part
# of the live Generate path via app/post_passes/cpsat_post_passes.py.
# It's still importable here for research scripts; this file just
# doesn't need to install it separately since production already does.

# (currently no research-only packages; placeholder for future
# additions like jupyter, networkx, etc.)
